"use client";

import React, { useState } from "react";
import ModeSide from "./ModeSide";
import PeakMode from ".";

export function TaskDetail({ ...props }) {
  const [isOpen, setIsOpen] = useState(false);

  return <div>this is so cool</div>;
}
